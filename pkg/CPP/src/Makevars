PKG_LIBS = $(shell Rscript -e 'Rcpp:::LdFlags()' )
PKG_CXXFLAGS = $(shell Rscript -e 'Rcpp:::CxxFlags()' )

# don't need c++0x in this package
# CLINK_CPPFLAGS = $(shell Rscript -e 'Rcpp:::Cxx0xFlags()' )
